"""
This file is kept for reference only.
The actual backend is located at: ../backend/app.py

To run the backend, use:
    cd ../backend
    python app.py

The backend runs on http://127.0.0.1:5000 and provides:
    GET  /api/test           - Health check
    POST /api/upload         - Upload a video file
    GET  /api/output/<name>  - Serve a processed video
"""
